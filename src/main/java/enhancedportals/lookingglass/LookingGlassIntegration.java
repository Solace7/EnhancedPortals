package enhancedportals.lookingglass;

/**
 * All this class does is get the LookingGlass "view-2" API instance
 */

public class LookingGlassIntegration
{
    /*public static void register(APIInstanceProvider provider) {
        LogHelper.info("Looking Glass API Provided");

        try{
            Object viewAPI = provider.getAPIInstance("view-2");

            if(FMLCommonHandler.instance().getEffectiveSide() == Side.CLIENT){
                LookingGlassIntegrateView2.onAPIGetClient(viewAPI);
            }
        } catch(APIUndefined exception)
        {
            LogHelper.catching(exception);
            LogHelper.warn(exception);
        }catch(APIVersionUndefined exception)
        {
            LogHelper.catching(exception);
            LogHelper.warn("Integration failed");
        }catch(APIVersionRemoved exception)
        {
            LogHelper.catching(exception);
            LogHelper.warn("Version of looking glass is no longer used, please update. Integration failed");
        }
    }*/

}
